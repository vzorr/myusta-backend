image: node:18

pipelines:
  branches:
    dev:
      - step:
          name: Deploy to dev server
          script:
            - echo "Deploying to server..."
            - >
              ssh -o StrictHostKeyChecking=no root@47.254.134.96 << 'EOF'
              cd myusta-backend
              git pull origin dev
              pm2 restart all || node index.js &
              EOF
