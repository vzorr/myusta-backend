image: node:20

pipelines:
  branches:
    dev:
      - step:
          name: 🚀 PM2 Auto Deploy
          script:
            - mkdir -p ~/.ssh
            - echo "$DEPLOY_KEY" > ~/.ssh/id_rsa
            - chmod 600 ~/.ssh/id_rsa
            - echo "$KNOWN_HOSTS" > ~/.ssh/known_hosts
            - eval "$(ssh-agent -s)"
            - ssh-add ~/.ssh/id_rsa
            - ssh -o StrictHostKeyChecking=no root@47.254.134.96 "bash /root/myusta-backend/deploy.sh"

